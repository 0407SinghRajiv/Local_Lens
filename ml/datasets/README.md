# Datasets Directory

This directory contains dataset caches and processing partitions:
- `raw/` - Unmodified ingested data (ignored by git)
- `processed/` - Cleaned, tokenized, and featurized tensors or parquet files
- `splits/` - Standardized train/validation/test indices
