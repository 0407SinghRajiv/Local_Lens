# Saved Models Directory

This directory stores serialized model binaries, checkpoint files (`*.pt`, `*.pth`, `*.joblib`, `*.pkl`, `*.onnx`), and tokenizer weights.

All model binary formats are ignored by version control. Export artifacts to remote storage (e.g. S3, GCS, HuggingFace Hub) for production deployment.
